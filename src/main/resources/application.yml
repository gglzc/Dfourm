spring:
  datasource:
    username: admin
    password: test
    url: jdbc:postgresql://0.0.0.0:5435/fourm
    driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

server:
  error:
    include-binding-errors: always
    include-message: always



